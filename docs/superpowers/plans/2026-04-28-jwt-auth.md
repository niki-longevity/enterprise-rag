# JWT 用户认证 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Agent 服务增加 JWT 用户认证（注册/登录），替代当前明文虚拟 userId。

**Architecture:** 新建 `src/auth/deps.py` 提供 `get_current_user` FastAPI Depends（解析 Bearer token → user_id + 设 contextvar），新建 `src/api/auth.py` 提供 register/login 路由，前端新增 Login.tsx 组件，App.tsx 加登录态判断。

**Tech Stack:** PyJWT, passlib[bcrypt], FastAPI Depends, React + Ant Design

---

### Task 1: 添加依赖

**Files:**
- Modify: `agent-service/requirements.txt`

- [ ] **Step 1: 追加 PyJWT 和 passlib**

在 `agent-service/requirements.txt` 末尾追加两行：

```txt
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
```

- [ ] **Step 2: 安装依赖**

```bash
C:/Users/15219/.conda/envs/pytorch/python.exe -m pip install PyJWT passlib[bcrypt]
```

Expected: `Successfully installed PyJWT-... passlib-... bcrypt-...`

- [ ] **Step 3: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add agent-service/requirements.txt && git commit -m "deps: add PyJWT and passlib for JWT auth"
```

---

### Task 2: 添加 JWT_SECRET 配置

**Files:**
- Modify: `agent-service/src/config/settings.py`

- [ ] **Step 1: 在 Settings 类中追加 jwt_secret 字段**

在 `agent-service/src/config/settings.py` 的 `Settings` 类末尾追加：

```python
    # JWT 认证
    jwt_secret: str = os.getenv("JWT_SECRET", "ea-jwt-secret-change-in-prod")
    jwt_algorithm: str = "HS256"
    jwt_expire_days: int = 7
```

- [ ] **Step 2: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add agent-service/src/config/settings.py && git commit -m "config: add JWT settings fields"
```

---

### Task 3: 新增 User 模型

**Files:**
- Modify: `agent-service/src/db/models.py`

- [ ] **Step 1: 在 models.py 追加 User 类**

在 `ChatHistory` 类之后追加：

```python
class User(Base):
    """用户实体"""
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, autoincrement=True)
    username = Column(String(50), nullable=False, unique=True)
    password_hash = Column(String(200), nullable=False)
    created_at = Column(DateTime, server_default=func.now())
```

- [ ] **Step 2: 创建 users 表**

用 Python 脚本执行建表（利用现有 engine）：

```bash
cd D:/Project/employee_assistant_Agent/agent-service && C:/Users/15219/.conda/envs/pytorch/python.exe -c "
from src.db.session import engine
from src.db.models import Base, User
Base.metadata.create_all(bind=engine)
print('users table created')
"
```

Expected: `users table created`（如果表已存在则不报错）

- [ ] **Step 3: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add agent-service/src/db/models.py && git commit -m "feat: add User model and users table"
```

---

### Task 4: 新建 auth 模块（contextvar + get_current_user）

**Files:**
- Create: `agent-service/src/auth/__init__.py`
- Create: `agent-service/src/auth/deps.py`

- [ ] **Step 1: 创建 __init__.py**

```python
# auth 模块
```

- [ ] **Step 2: 创建 deps.py**

```python
# JWT 鉴权依赖 + 成本追踪用 contextvar
import contextvars
import jwt
from fastapi import Header, HTTPException
from src.config.settings import settings

# 成本追踪用 contextvar（模块二用，先进 auth）
_tracking_ctx: contextvars.ContextVar = contextvars.ContextVar('tracking', default=None)

__all__ = ["get_current_user", "_tracking_ctx"]


async def get_current_user(authorization: str = Header(...)) -> str:
    """从 Authorization: Bearer <token> 解析 JWT，返回 user_id

    同时设置 _tracking_ctx，供后续 LLM 成本追踪回调使用（模块二）。
    """
    try:
        scheme, token = authorization.split(" ", 1)
        if scheme.lower() != "bearer":
            raise ValueError("Not a Bearer token")
        payload = jwt.decode(token, settings.jwt_secret, algorithms=[settings.jwt_algorithm])
        user_id = str(payload["user_id"])
        _tracking_ctx.set({"user_id": user_id, "session_id": None, "node_type": None})
        return user_id
    except (jwt.ExpiredSignatureError, jwt.InvalidTokenError, ValueError, KeyError):
        raise HTTPException(status_code=401, detail="Invalid or expired token")
```

- [ ] **Step 3: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add agent-service/src/auth/__init__.py agent-service/src/auth/deps.py && git commit -m "feat: add JWT auth dependency with contextvar"
```

---

### Task 5: 新建 auth API 路由（register + login）

**Files:**
- Create: `agent-service/src/api/auth.py`

- [ ] **Step 1: 创建 auth.py**

```python
# 认证API路由 - 注册/登录
import datetime
import jwt
from fastapi import APIRouter, HTTPException
from sqlalchemy.orm import Session
from passlib.hash import bcrypt
from pydantic import BaseModel, Field

from src.db.session import SessionLocal
from src.db.models import User
from src.db.mapper import BaseMapper
from src.config.settings import settings

router = APIRouter()


class AuthRequest(BaseModel):
    username: str = Field(..., min_length=3, max_length=20)
    password: str = Field(..., min_length=6, max_length=50)


def _create_token(user: User) -> str:
    """生成 JWT，有效期 7 天"""
    payload = {
        "user_id": user.id,
        "username": user.username,
        "exp": datetime.datetime.utcnow() + datetime.timedelta(days=settings.jwt_expire_days),
    }
    return jwt.encode(payload, settings.jwt_secret, algorithm=settings.jwt_algorithm)


@router.post("/register")
def register(req: AuthRequest):
    """注册新用户，返回 JWT token"""
    db = SessionLocal()
    try:
        mapper = BaseMapper(User, db)
        existing = mapper.list_by_field("username", req.username)
        if existing:
            raise HTTPException(status_code=409, detail="Username already exists")

        user = User(
            username=req.username,
            password_hash=bcrypt.hash(req.password),
        )
        mapper.save(user)
        return {"token": _create_token(user)}
    finally:
        db.close()


@router.post("/login")
def login(req: AuthRequest):
    """登录，返回 JWT token"""
    db = SessionLocal()
    try:
        mapper = BaseMapper(User, db)
        users = mapper.list_by_field("username", req.username)
        if not users or not bcrypt.verify(req.password, users[0].password_hash):
            raise HTTPException(status_code=401, detail="Invalid username or password")

        return {"token": _create_token(users[0])}
    finally:
        db.close()
```

- [ ] **Step 2: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add agent-service/src/api/auth.py && git commit -m "feat: add auth API routes (register/login)"
```

---

### Task 6: 修改 chat.py — 去掉明文 userId，改用 JWT Depends

**Files:**
- Modify: `agent-service/src/api/chat.py`

- [ ] **Step 1: 导入 get_current_user**

在 `agent-service/src/api/chat.py` 顶部 import 区域追加：

```python
from src.auth.deps import get_current_user
```

- [ ] **Step 2: ChatRequest 去掉 userId 字段**

将第 57-61 行的 `ChatRequest` 改为：

```python
class ChatRequest(BaseModel):
    """前端发来的对话请求"""
    message: str = Field(..., description="用户消息内容")
    sessionId: Optional[str] = Field(None, description="会话ID，可选，用于会话续传")
```

- [ ] **Step 3: /chat 路由加 Depends(get_current_user)**

将 `/chat` 路由（第 156-162 行）改为：

```python
@router.post("/chat")
async def chat(
    request: ChatRequest,
    user_id: str = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """流式对话接口"""
    return await chat_stream_impl(user_id, request.message, request.sessionId, db)
```

- [ ] **Step 4: /history 路由加 Depends(get_current_user)**

将 `/history` 路由（第 165-172 行）改为：

```python
@router.get("/history")
def get_history(session_id: str, user_id: str = Depends(get_current_user)):
    """获取指定会话的历史消息"""
    db = SessionLocal()
    mapper = ChatHistoryMapper(db)
    history = mapper.list_by_session_id(session_id)
    db.close()
    return history
```

- [ ] **Step 5: /sessions 路由改用 Depends(get_current_user)**

将 `/sessions` 路由（第 175-182 行）改为：

```python
@router.get("/sessions")
def get_sessions(user_id: str = Depends(get_current_user)):
    """获取用户的会话ID列表，按最后消息时间倒序"""
    db = SessionLocal()
    mapper = ChatHistoryMapper(db)
    sessions = mapper.list_session_ids_by_user_id(user_id)
    db.close()
    return sessions
```

- [ ] **Step 6: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add agent-service/src/api/chat.py && git commit -m "feat: enforce JWT auth on chat/history/sessions endpoints"
```

---

### Task 7: main.py 注册 auth 路由

**Files:**
- Modify: `agent-service/src/main.py`

- [ ] **Step 1: 注册 auth router**

在 `agent-service/src/main.py` 中，`from src.api import chat, webhook` 行后追加 import：

将第 5 行改为：

```python
from src.api import chat, webhook, auth
```

在 `app.include_router(webhook.router, ...)` 行后追加：

```python
app.include_router(auth.router, prefix="/api", tags=["auth"])
```

- [ ] **Step 2: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add agent-service/src/main.py && git commit -m "feat: register auth router in main app"
```

---

### Task 8: 新建前端登录/注册组件

**Files:**
- Create: `frontend/src/Login.tsx`

- [ ] **Step 1: 创建 Login.tsx**

```tsx
import { useState } from 'react'
import { Input, Button, Tabs, message } from 'antd'
import { UserOutlined, LockOutlined } from '@ant-design/icons'

interface Props {
  onLogin: (token: string) => void
}

function Login({ onLogin }: Props) {
  const [username, setUsername] = useState('')
  const [password, setPassword] = useState('')
  const [loading, setLoading] = useState(false)
  const [tab, setTab] = useState('login')

  async function handleSubmit() {
    if (!username.trim() || !password.trim()) {
      message.warning('请输入用户名和密码')
      return
    }
    setLoading(true)
    try {
      const endpoint = tab === 'login' ? '/api/auth/login' : '/api/auth/register'
      const res = await fetch(endpoint, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ username: username.trim(), password }),
      })
      if (!res.ok) {
        const err = await res.json()
        message.error(err.detail || '请求失败')
        return
      }
      const data = await res.json()
      localStorage.setItem('token', data.token)
      onLogin(data.token)
    } catch {
      message.error('网络错误，请重试')
    } finally {
      setLoading(false)
    }
  }

  function handleKeyDown(e: React.KeyboardEvent) {
    if (e.key === 'Enter') handleSubmit()
  }

  return (
    <div style={{
      display: 'flex', justifyContent: 'center', alignItems: 'center',
      height: '100vh', background: '#f0f2f5'
    }}>
      <div style={{
        width: 360, padding: 32, background: '#fff',
        borderRadius: 8, boxShadow: '0 2px 8px rgba(0,0,0,0.1)'
      }}>
        <h2 style={{ textAlign: 'center', marginBottom: 24 }}>企业员工助手</h2>
        <Tabs
          activeKey={tab}
          onChange={setTab}
          centered
          items={[
            { key: 'login', label: '登录' },
            { key: 'register', label: '注册' },
          ]}
        />
        <Input
          prefix={<UserOutlined />}
          placeholder="用户名"
          value={username}
          onChange={e => setUsername(e.target.value)}
          onKeyDown={handleKeyDown}
          style={{ marginBottom: 12 }}
        />
        <Input.Password
          prefix={<LockOutlined />}
          placeholder="密码"
          value={password}
          onChange={e => setPassword(e.target.value)}
          onKeyDown={handleKeyDown}
          style={{ marginBottom: 24 }}
        />
        <Button
          type="primary"
          block
          loading={loading}
          onClick={handleSubmit}
        >
          {tab === 'login' ? '登录' : '注册'}
        </Button>
      </div>
    </div>
  )
}

export default Login
```

- [ ] **Step 2: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add frontend/src/Login.tsx && git commit -m "feat: add Login/Register component"
```

---

### Task 9: 修改前端 api.ts — 去掉 userId，加 Authorization header

**Files:**
- Modify: `frontend/src/api.ts`

- [ ] **Step 1: 修改 sendMessageStream 函数签名和请求体**

将 `api.ts` 中的 `sendMessageStream` 改为：

```typescript
export async function sendMessageStream(
  message: string,
  sessionId: string | undefined,
  onChunk: (chunk: string) => void,
  onEnd: (sessionId: string) => void,
  onError: (error: string) => void
) {
  const token = localStorage.getItem('token') || ''
  const res = await fetch(`${API_BASE}/chat`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`,
    },
    body: JSON.stringify({ message, sessionId: sessionId || null }),
  })
  // ... 后续 reader 逻辑不变
```

（原函数的 `userId: string` 参数和 `body` 中的 `userId` 字段去掉，其余 reader/decoder 逻辑完全保留）

- [ ] **Step 2: 修改 getSessions 函数**

```typescript
export async function getSessions() {
  const token = localStorage.getItem('token') || ''
  const res = await fetch(`${API_BASE}/sessions`, {
    headers: { 'Authorization': `Bearer ${token}` },
  })
  if (!res.ok) throw new Error(`请求失败: ${res.status}`)
  return res.json()
}
```

- [ ] **Step 3: 修改 getHistory 函数（加 Authorization header）**

```typescript
export async function getHistory(sessionId: string) {
  const token = localStorage.getItem('token') || ''
  const res = await fetch(`${API_BASE}/history?session_id=${encodeURIComponent(sessionId)}`, {
    headers: { 'Authorization': `Bearer ${token}` },
  })
  if (!res.ok) throw new Error(`请求失败: ${res.status}`)
  return res.json()
}
```

- [ ] **Step 4: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add frontend/src/api.ts && git commit -m "feat: api.ts - remove userId, add JWT Authorization header"
```

---

### Task 10: 修改 App.tsx — 加登录态判断

**Files:**
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: 导入 Login 组件**

在 `frontend/src/App.tsx` 顶部 import 区域追加：

```tsx
import Login from './Login'
```

- [ ] **Step 2: 删除 USER_ID 常量**

删除第 16 行：

```
const USER_ID = 'user001'
```

- [ ] **Step 3: 加登录态和 token state**

在 `App` 函数开头追加：

```tsx
const [token, setToken] = useState<string>(localStorage.getItem('token') || '')
```

- [ ] **Step 4: 修改 loadSessions — 去 USER_ID 参数**

```tsx
async function loadSessions() {
  try {
    const data = await getSessions()  // 不再传 USER_ID
    setSessions(data)
    if (data.length > 0 && !activeSession) {
      setActiveSession(data[0])
    }
  } catch {
    // 401 等错误在 api.ts 层不抛异常会静默失败，这里仍兜底
  }
}
```

- [ ] **Step 5: 修改 handleSend — 去 USER_ID 参数**

将 `sendMessageStream` 调用改为：

```tsx
await sendMessageStream(
  text,
  activeSession || undefined,
  (chunk) => { ... },  // 不变
  (sessionId) => { ... },  // 不变
  (error) => { ... }  // 不变
)
```

- [ ] **Step 6: 在 return 前加登录守卫**

在 `return` 语句最前面加：

```tsx
if (!token) {
  return <Login onLogin={(t: string) => setToken(t)} />
}
```

- [ ] **Step 7: 加退出登录按钮**

在 `chat-header` div 内追加一个退出按钮（放在标题右边）：

```tsx
<div className="chat-header">
  企业员工助手
  <Button
    size="small"
    style={{ float: 'right', marginTop: 4 }}
    onClick={() => {
      localStorage.removeItem('token')
      setToken('')
      setSessions([])
      setActiveSession('')
      setMessages([])
    }}
  >
    退出
  </Button>
</div>
```

- [ ] **Step 8: Commit**

```bash
cd D:/Project/employee_assistant_Agent && git add frontend/src/App.tsx && git commit -m "feat: add login guard and logout to App"
```

---

### Task 11: 端到端验证

- [ ] **Step 1: 启动后端**

```bash
cd D:/Project/employee_assistant_Agent/agent-service && C:/Users/15219/.conda/envs/pytorch/python.exe -m src.main
```

- [ ] **Step 2: 测试无 token 请求返回 401**

```bash
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8001/api/chat -H "Content-Type: application/json" -d '{"message":"你好"}'
```

Expected: `401`

- [ ] **Step 3: 测试注册**

```bash
curl -s -X POST http://localhost:8001/api/auth/register -H "Content-Type: application/json" -d '{"username":"testuser","password":"123456"}'
```

Expected: `{"token":"eyJ..."}`

- [ ] **Step 4: 测试带 token 请求 /chat**

```bash
TOKEN=<上一步返回的token>
curl -s -X POST http://localhost:8001/api/chat -H "Content-Type: application/json" -H "Authorization: Bearer $TOKEN" -d '{"message":"你好"}'
```

Expected: SSE 流式响应

- [ ] **Step 5: 测试 /history 和 /sessions**

```bash
curl -s "http://localhost:8001/api/sessions" -H "Authorization: Bearer $TOKEN"
curl -s "http://localhost:8001/api/history?session_id=sess_testuser_xxx" -H "Authorization: Bearer $TOKEN"
```

Expected: 返回 JSON 数组

- [ ] **Step 6: 启动前端验证登录 UI**

```bash
cd D:/Project/employee_assistant_Agent/frontend && npm run dev
```

浏览器访问 → 出现登录/注册页面 → 注册 → 自动进入聊天界面 → 发送消息正常流式回复 → 退出登录回到登录页

- [ ] **Step 7: 验证重复用户名注册返回 409**

```bash
curl -s -X POST http://localhost:8001/api/auth/register -H "Content-Type: application/json" -d '{"username":"testuser","password":"123456"}'
```

Expected: `{"detail":"Username already exists"}`

- [ ] **Step 8: 验证错误密码登录返回 401**

```bash
curl -s -X POST http://localhost:8001/api/auth/login -H "Content-Type: application/json" -d '{"username":"testuser","password":"wrongpassword"}'
```

Expected: `{"detail":"Invalid username or password"}`
